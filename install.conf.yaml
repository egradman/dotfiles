- clean: ['~']
- link:
    ~/.tmux: tmux
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.config/nvim: nvim
- shell:
    - [git submodule update --init --recursive, Installing submodules]
